import React from 'react'
import { CustomText } from './styles'

export const TextInfo = ({ children }) => {
    return (
        <CustomText>
            {children}
        </CustomText>
    )
}


  